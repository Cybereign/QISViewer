function applyDisplayConfig() {
    var modules = displayConfig.modules || {};
    var map = {
        header: 'header',
        statusBar: 'statusBar',
        filterBar: 'filterBar',
        kpiGrid: 'kpiGrid',
        statsTable: 'statsWrapper',
        targetChart: 'targetChartContainer',  // ★★★ 新增 ★★★
        targetChart2: 'targetChartContainer2', // ★★★ 新增 ★★★
        chartGrid: 'chartGrid',
        tableWrapper: 'tableWrapper',
        footerNote: 'footerNote',
        fileUploadArea: 'fileUploadArea',
        loadingIndicator: 'loadingIndicator',
        errorMsg: 'errorMsg'
    };
    // ... 后续代码不变
}
